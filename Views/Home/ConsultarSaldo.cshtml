@{
    ViewData["Title"] = "Tu Saldo";
}

<h2>Tu Saldo :)</h2>

@if (ViewData["Error"] != null)
{
    <div class="alert alert-danger">
        @ViewData["Error"]
    </div>
}
else
{
    <div class="alert alert-success">
        <p>Tu saldo actual es: @ViewData["VerSaldo"]</p>
    </div>
}
